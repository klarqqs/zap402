/**
 * Inner padding for `/network` content inside `kofi-dashboard-card` — matches Terminal tab panels.
 */
export const NETWORK_TAB_PANEL_PAD = "px-1 pb-6 pt-5 md:px-4";

/**
 * Legacy: centered column when not inside the Terminal-style card (unused on Network page).
 */
export const NETWORK_PAGE_INNER =
  "mx-auto w-full min-w-0 max-w-4xl px-4 md:px-6";
